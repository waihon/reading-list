{{one-way-input
  title
  update=(action (mut title))
  keyEvents=(hash
    13=(action 'onEnter')
  )
}}

<ul>
{{#each books as |book|}}
  <li>{{ book.title }} <button {{action removeBook book}}>-</button></li>
{{/each}}
</ul>
